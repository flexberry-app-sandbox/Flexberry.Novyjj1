import { Serializer as ЖурУчВыпРабSerializer } from
  '../mixins/regenerated/serializers/i-i-s-novyjj1-жур-уч-вып-раб';
import __ApplicationSerializer from './application';

export default __ApplicationSerializer.extend(ЖурУчВыпРабSerializer, {
  /**
  * Field name where object identifier is kept.
  */
  primaryKey: '__PrimaryKey'
});
